{
  "name": "Tableau-Fabric-AI-Bridge",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "postCreateCommand": "curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash && sudo apt-get install -y git-lfs && git lfs install && pip install requests azure-identity azure-keyvault-secrets --break-system-packages"
}
